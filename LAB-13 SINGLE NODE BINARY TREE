# Construct a binary tree with a single node
# ARYAN 29 B13

class TreeNode:

	def __init__(self,data):
		self.data = data
		self.left = None
		self.right = None

	def printhead(self):
		print(self.data)

treenode = TreeNode(35)
treenode.printhead()
